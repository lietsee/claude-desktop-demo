# Claude Desktop実演用プロンプト集

上から順に使用します。すべてを一度に貼らず、各フェーズの回答を確認してから次へ進んでください。

---

# 1. ファイル構成の把握

```text
Project knowledgeまたはこのチャットに追加されている資料を確認してください。

まだデータ分析、集計、ファイル作成は始めないでください。

次だけを簡潔に報告してください。

1. 認識したファイル名
2. 各ファイルの役割
3. 今回依頼されている仕事の概要
4. 不足している可能性がある資料

要件書とCSVを区別してください。
```

## 認識漏れがある場合

```text
認識したファイルをもう一度確認してください。売上CSVが3つ、返金CSVが1つ、要件書が1つある想定です。見えていないファイル名だけを報告してください。
```

---

# 2. 調査のみ

```text
元CSVと要件書を横断して調査してください。

この段階では、Excel、PDF、Artifact、変換後CSVなどの成果物を作成しないでください。元データも変更しないでください。

次を報告してください。

1. 各CSVの列構成
2. ファイル間の列名対応
3. 日付形式の違い
4. 金額形式の違い
5. 支店名の表記揺れ
6. 空欄、重複、不正値、異常値の候補
7. 返金データと元売上取引の関係
8. 要件書から確定できる処理ルール
9. 要件書だけでは確定できない事項

報告では「元データから確認した事実」「要件書で確定したルール」「あなたの推測」「人間への確認事項」を分けてください。

異常候補には、元ファイル名、CSV上の行番号、取引ID、理由を付けてください。
```

---

# 3. 見落とし探索

調査が浅い場合だけ使用します。

```text
先ほどの調査を批判的に再確認してください。

特に次を、元CSV全行から再探索してください。

- 全項目が同じ完全重複
- 同じ取引IDだが内容が異なる矛盾重複
- 実在しない日付
- 整数化できない金額
- 対象外支店
- 元取引が存在しない返金
- 正数と負数が混在する返金
- 0円売上
- 空欄カテゴリー
- 空欄来店人数

「変換すれば正常に採用できる表記揺れ」と「採用できないエラー」を分けてください。
まだ成果物は作成しないでください。
```

---

# 4. Excel・PDF作成

調査結果が妥当なことを確認してから使用します。

```text
調査結果と reference/requirements.md の要件に従い、元CSVを統合してください。

コード実行とファイル作成機能を使用し、次の2ファイルを作成してください。

1. integrated_sales.xlsx
2. management_report.pdf

重要な条件:

- 元CSVは変更しない
- 推測で元値を修正しない
- 判断不能な行はエラー一覧へ残す
- 完全重複と矛盾重複を区別する
- 返金金額は入力の符号にかかわらず負数にする
- 正常採用された元売上が存在しない返金はエラーにする
- 元ファイル名と元行番号を正規化データへ残す

Excelには次のシートを作成してください。

- 正規化データ
- 支店別集計
- エラー一覧
- 検証結果
- 処理ルール

Excelは、見出し固定、フィルター、読みやすい列幅、円表記、適切な折り返しを設定してください。

PDFは経営者向けに、処理概要、支店別集計、全体合計、エラー、注意点、検証結果を簡潔にまとめてください。

作成後、元CSVから件数と金額を独立に再計算し、次を検証してください。

- 入力行数 = 採用行数 + 完全重複除外行数 + エラー行数
- 支店別純売上額の合計 = 全体純売上額
- ExcelとPDFの主要数値が一致
- 全返金が負数
- エラー対象行が正規化データに混ざっていない

検証に失敗した場合は修正し、成功してからファイルと結果を提示してください。

最後に、作成ファイル、採用件数、完全重複件数、エラー件数、全体純売上額、未解決事項を報告してください。
```

---

# 5. 成果物の再検証

```text
今作成したExcelとPDFを、作成時の完了報告を前提にせず再検証してください。

1. integrated_sales.xlsx を開き、全シートの存在と内容を確認
2. 元CSVだけから件数と支店別金額を再計算
3. Excelの正規化データと支店別集計を照合
4. management_report.pdf の主要数値をExcelと照合
5. Excelのエラー一覧に、採用不能行がすべて根拠付きで記録されているか確認
6. 日本語文字化け、表の欠落、列幅、PDFのはみ出しを確認

結果を「成功」「失敗」「人間による確認が必要」に分けて報告してください。
失敗があれば、数値ロジックを勝手に変えず、原因を特定して修正してください。
```

## Excelの見た目だけ直す

```text
集計ロジックと数値は変更せず、integrated_sales.xlsx の可読性だけを改善してください。見出し固定、フィルター、列幅、円表記、折り返し、支店別集計の視認性を確認し、再生成後に開けることを検証してください。
```

## PDF文字化け対応

```text
management_report.pdf を日本語対応の方法で再生成してください。生成後に全ページを確認し、文字化け、欠落、重なり、ページ外へのはみ出しがないことを報告してください。数値はExcelから変更しないでください。
```

---

# 6. Artifactダッシュボード

```text
検証済みの integrated_sales.xlsx の「正規化データ」「支店別集計」「エラー一覧」を基準に、インタラクティブなArtifactダッシュボードを作成してください。

元CSVを新たに解釈し直さず、Excelの検証済み数値を使用してください。

表示する内容:

- 全体の純売上額
- 支店別の純売上額
- 売上件数
- 返金件数
- 来店人数
- エラー件数
- 正規化済み取引一覧

操作:

- 全支店／東京支店／名古屋支店／大阪支店の絞り込み
- 純売上額または取引金額による並び替え
- 売上／返金の絞り込み
- 返金行を視覚的に区別

要件:

- フィルター適用後はKPIも表示中データから再計算する
- 円表記と件数を読みやすくする
- 狭い画面幅でも主要情報を読めるようにする
- サンプル値や仮データを入れない
- 秘密情報や外部サービスの認証情報を埋め込まない

作成後、全支店と各支店の主要数値をExcelと照合し、一致結果を報告してください。
```

## Artifactの数字がずれる場合

```text
Artifact内のデータと計算状態をすべて再確認してください。サンプル値や古い値を削除し、integrated_sales.xlsx の検証済み正規化データだけを基準に再構築してください。

全支店、各支店、返金のみの状態で、純売上額、件数、来店人数をExcelと照合してください。
```

---

# 7A. Google Driveへ保存

Google Driveコネクタを接続している場合だけ使用します。

```text
作成済みの integrated_sales.xlsx と management_report.pdf をGoogle Driveへ保存したいです。

書き込み前に、利用可能な保存先を確認し、保存予定のフォルダ、ファイル名、同名ファイルが存在する場合の扱いを報告してください。

私が保存先を確認するまでは、アップロードや上書きを実行しないでください。
```

保存先確認後:

```text
確認したデモ用フォルダへ2ファイルを保存してください。既存ファイルは上書きせず、同名がある場合は日時または連番を付けてください。保存後、実際のファイル名と保存先を報告してください。
```

---

# 7B. Gmail下書き

Gmailコネクタを接続している場合だけ使用します。

```text
management_report.pdf の内容を社内へ共有するメール下書きを作成してください。

送信はしないでください。

本文には、対象期間、全体純売上額、支店別概要、エラー件数、確認してほしい事項を簡潔に含めてください。

宛先が不明なため、まず件名と本文案だけを提示してください。添付する予定のファイル名も示してください。
```

---

# 7C. ローカルフォルダへ保存

Desktop ExtensionまたはCoworkで、デモ用フォルダへのアクセスを許可した場合だけ使用します。

```text
許可されている claude-desktop-demo フォルダの範囲だけを使用してください。

元の input フォルダは変更せず、作成済みの integrated_sales.xlsx と management_report.pdf を output フォルダへ保存してください。

書き込み前に対象パスを報告し、input 内へ書き込まないことを確認してください。
```

---

# 7D. Coworkで表計算アプリを確認

コンピュータ操作を利用できる場合だけ使用します。

```text
作成した integrated_sales.xlsx を表計算アプリで開いて確認してください。

確認すること:

- 5つのシートが存在する
- 見出し行が固定されている
- フィルターが設定されている
- 金額が円表記になっている
- 列幅と折り返しが読みやすい
- 支店別集計の主要数値が見える

内容変更や保存は行わず、表示確認だけをしてください。
```

---

# 8. 敵対的レビュー

新しいチャットで使用します。元CSV、要件書、Excel、PDFを参照できる状態にしてください。

```text
あなたは、この成果物を作成した担当者とは別の内部監査担当です。

作成担当者の説明、会話、完了報告、集計値を信用しないでください。
次だけを根拠に独立検証してください。

- 元CSV 4ファイル
- reference/requirements.md
- integrated_sales.xlsx
- management_report.pdf

最初に元CSVだけから、採用、完全重複除外、エラーを判定し、件数と支店別純売上額を独立に再計算してください。

その後、次を検証してください。

- 二重計上
- 正常行の欠落
- エラー行の混入
- 返金符号
- 日付と金額の変換
- 矛盾重複の扱い
- 元取引不明返金の扱い
- 支店別合計と全体合計
- Excel内シート間の整合
- ExcelとPDFの整合
- 要件書からの逸脱

指摘は「重大」「中程度」「軽微」に分類してください。
各指摘には、元ファイルと行、Excelのシートと該当箇所、要件書の該当ルール、再現方法を付けてください。

実害のない好みや、根拠のない改善案は指摘しないでください。
問題がない項目も、何を確認したか明記してください。
```

## 監査指摘の根拠を再確認

```text
指摘を採用する前に、各指摘について次を示してください。

1. 要件書の該当ルール
2. 元CSVの該当行
3. 期待される計算または処理
4. 成果物の実際の値
5. 差異

根拠が不足する指摘は撤回してください。
```

---

# 9. Project instructions作成

```text
今回の作業から、翌月以降も変わらない仕事の原則だけを抽出し、Claude ProjectのProject instructionsへ貼れる文章を作成してください。

含める内容:

- 元データを変更しない
- 作業前に資料構成と異常値を調査する
- 事実、推測、確認事項を分ける
- 要件書を優先する
- 判断不能行を黙って削除・補完しない
- エラー行を根拠付きで分離する
- 件数と金額を独立に検証する
- 作成ファイルを再度開いて確認する
- 完了報告に未解決事項を含める

含めない内容:

- 今回だけのファイル名
- 今回だけの金額や件数
- 今回だけの支店名
- 長大な要件書の全文

簡潔で、別の月の同種業務にも使える文章にしてください。
```

---

# 10. 翌月処理テンプレート

```text
翌月の売上CSVを処理するときに、新しいチャットの最初へ貼る定型プロンプトを作成してください。

テンプレートは次の流れを強制してください。

1. 入力ファイルと要件書を確認
2. 前月との列構成差を調査
3. 異常値と曖昧な点を報告
4. まだ成果物を作らず処理計画を提示
5. 人間の承認後にExcelとPDFを作成
6. 元データから独立検証
7. 新しいチャットで監査できる成果物一覧を提示

月、対象ファイル、出力名などはプレースホルダーにしてください。
```

---

# 11. 最終振り返り

```text
今回の作業を、非エンジニア向けに振り返ってください。

次を整理してください。

- Claudeへ渡した資料
- 人間が判断したこと
- Claudeが自動化したこと
- 作成した成果物
- 実施した検証
- コネクタやArtifactが役立った場面
- AIへ任せきりにしてはいけない点
- 次回から再利用できる手順

機能名の羅列ではなく、実際の業務がどう変わるかを中心に説明してください。
```
